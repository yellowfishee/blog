+++
title = "RabbitMQ"
date = 2023-03-06
lastmod = 2023-03-08T18:48:01+08:00
tags = ["RabbitMQ", "Java", "分布式"]
draft = false
katex = true
[menu]
  [menu.main]
    identifier = "rabbitmq"
    parent = "docs"
    weight = 3
+++

## RabbitMQ消息队列 {#rabbitmq消息队列}


### 消息队列 {#消息队列}

在之前，我们如果需要进行远程调用，那么一般可以通过发送HTTP请求来完成，而现在，我们可以使用第二种方式，就是消息队列的方式，它能够将发送方发送的信息放入消息队列中，当新的消息入队的时候，会通知接收方进行处理，一般消息发送方称为生产者，接收方称为发送者。
![](/ox-hugo/2023-03-06_01-07-18_screenshot.png)

这样我们的所有的请求，都可以直接丢到消息队列里面去了，再由消费者取出，不再是连接消费者的形式了，而是加了一个中间商，这也是一种很好的解耦方案，并且在高并发的情况下，由于消费者能力有限，消息队列也能起到一个削峰填谷的作用，堆积一部分的请求，再由消费者来慢慢处理，而不会像直接调用哪样请求蜂拥而至。

那么现在常用的消息队列实现有哪些呢：

-   RabbitMQ
-   Kafka
-   RocketMQ


### 安装消息队列 {#安装消息队列}

这里直接有一个链接，后续再补充在这里面。


#### 下载镜像 {#下载镜像}

这里使用的安装方式是docker安装，实体机装多了麻烦。

```shell
docker pull rabbitmq

docker images
```


#### 创建并启动容器 {#创建并启动容器}

这里直接略过安装步骤
